version: 2
jobs:
  build:
    machine: true
    steps:
      - add_ssh_keys: 
          fingerprints:
            - "52:d6:3d:21:10:e2:d2:54:82:37:84:43:92:a5:5a:62"
      - checkout
      - run: git config --global user.name "ryolairyolai"
      - run: git clone https://github.com/ryolairyolai/test
      - run: cd test/
      - run: git remote add upstream https://github.com/na-ka-mu-ra-/test
      - run: git checkout master
      - run: git pull upstream master
      - run: git push origin master

